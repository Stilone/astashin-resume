export interface IconList {
  name: string;
  svg: string
  link: string;
}

export interface imageList {
  name: string;
  link: string;
}

export interface techList {
  name: string;
  svg: string;
}